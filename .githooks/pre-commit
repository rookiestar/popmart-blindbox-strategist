#!/bin/sh
set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

PYTHONDONTWRITEBYTECODE=1 python3 scripts/public_check.py --index
PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests
